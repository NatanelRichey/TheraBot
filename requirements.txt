# TheraBot RAG Deployment Requirements
# For HuggingFace Space deployment with llama.cpp
# Minimal dependencies for production RAG system
#
# Note: If llama-cpp-python wheel mismatch occurs on your Space, set env vars:
#   PIP_NO_BINARY=llama-cpp-python
#   CMAKE_ARGS=-DLLAMA_BLAS=ON -DLLAMA_BLAS_VENDOR=OpenBLAS

# =============================================================================
# CORE ML/AI
# =============================================================================
# Sentence embeddings for RAG
sentence-transformers>=2.2.0,<5.2.0

# Transformers for tokenization (used by build_index.py)
transformers>=4.35.0

# =============================================================================
# VECTOR DATABASE
# =============================================================================
# FAISS for similarity search (CPU version for free tier)
faiss-cpu>=1.7.4,<2.0.0

# Alternative for GPU deployment (uncomment if using GPU Space)
# faiss-gpu>=1.7.4

# =============================================================================
# LLAMA.CPP
# =============================================================================
# llama.cpp Python bindings for inference (pin; Space may build from source)
llama-cpp-python==0.3.2

# =============================================================================
# WEB FRAMEWORK
# =============================================================================
# Gradio for chat UI
gradio>=4.0.0

# =============================================================================
# UTILITIES
# =============================================================================
# HTTP requests (for model downloads if needed)
requests>=2.31.0

# HuggingFace Hub (for downloading models from Model repositories)
huggingface_hub>=0.20.0

# Standard dependencies
numpy>=1.24.0
python-multipart>=0.0.6

# =============================================================================
# LLAMA SERVER RUNTIME DEPS
# =============================================================================
# Required by llama_cpp.server for Server-Sent Events
sse-starlette>=1.6.5
fastapi>=0.110
uvicorn>=0.23
starlette-context>=0.3.6
pydantic>=2.0.0
pydantic-settings>=2.0.0
orjson>=3.9.0

# =============================================================================
# OPTIONAL: RERANKING
# =============================================================================
# Uncomment if you want detailed retrieval path with reranking
# Note: Adds ~300MB download, increases latency significantly
# sentence-transformers already includes CrossEncoder support

# =============================================================================
# INSTALLATION
# =============================================================================
# pip install -r requirements_deploy.txt

# For free CPU Spaces, use faiss-cpu
# For GPU Spaces, uncomment faiss-gpu instead

# llama-cpp-python installation may need special flags on some systems
# If issues, try: CMAKE_ARGS="-DLLAMA_BLAS=ON -DLLAMA_BLAS_VENDOR=Generic" pip install llama-cpp-python

